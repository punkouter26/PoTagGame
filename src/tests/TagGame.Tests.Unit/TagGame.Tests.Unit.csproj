<Project Sdk="Microsoft.NET.Sdk">
  <!--
    TargetFramework, Nullable, ImplicitUsings, TreatWarningsAsErrors, and NoWarn
    are inherited from Directory.Build.props.
    Package versions are managed centrally in Directory.Packages.props (CPM).
  -->
  <PropertyGroup>
    <RootNamespace>PoTagGame.Tests.Unit</RootNamespace>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="Moq" />
    <PackageReference Include="FluentAssertions" />
  </ItemGroup>

  <ItemGroup>
    <!-- Reference the production project so tests compile against real types -->
    <ProjectReference Include="..\..\server\PoTagGame.csproj" />
  </ItemGroup>
</Project>
